name: Deploy to Vodien

on:
  push:
    branches: [main]
    paths:
      - 'wp-content/**'
      - '.github/workflows/**'
      - 'robots.txt'
      - 'version.php'
      - 'check-files.php'
      - '.htaccess'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy .htaccess and cleanup diagnostic files
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp
          echo "Deploying .htaccess and removing old diagnostic files:"
          lftp -e "set ssl:verify-certificate no; \
                   open -u ${{ secrets.VODIEN_USER }},${{ secrets.VODIEN_PASSWORD }} ${{ secrets.VODIEN_HOST }}; \
                   cd public_html/ah-ho-fruits; \
                   put .htaccess; \
                   rm -f phpinfo.php wp-test.php clear-opcache.php test-php.php; \
                   bye"
          echo "Deployment complete"

      - name: Deploy wp-content via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.VODIEN_HOST }}
          username: ${{ secrets.VODIEN_USER }}
          password: ${{ secrets.VODIEN_PASSWORD }}
          local-dir: ./wp-content/
          server-dir: public_html/ah-ho-fruits/wp-content/
          dangerous-clean-slate: false

      - name: Deploy root files (robots.txt only) via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.VODIEN_HOST }}
          username: ${{ secrets.VODIEN_USER }}
          password: ${{ secrets.VODIEN_PASSWORD }}
          local-dir: ./
          server-dir: public_html/ah-ho-fruits/
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/node_modules/**
            **/wp-content/**
            **/docs/**
            **/sql/**
            **/.env
            **/deploy-key*
            **/deploy.sh
            **/*.md
            **/*.yml
            **/*.sh
            **/docker-compose.yml
